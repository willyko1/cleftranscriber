# Optional: an existing sheet-music conversion service, NOT an AI model URL.
# It must accept multipart sheetMusic + clef and return PNG, JPEG, or WebP.
TRANSCRIPTION_API_URL=
# Optional bearer token; kept server-side.
TRANSCRIPTION_API_KEY=
